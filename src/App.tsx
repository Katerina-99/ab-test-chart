import { normalizeData } from "./utils/normalizeData";
import "./App.css";

function App() {
  console.log(normalizeData());

  return (
    <>
      <div>Check console</div>
    </>
  );
}

export default App;
